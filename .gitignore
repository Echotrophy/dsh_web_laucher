# 编译产物（用 build.cmd 生成，可通过 GitHub Releases 发布）
dsh-web-launcher.exe
# 旧版本残留（运行中实例仍在占用，退出工具后可手动删除）
dsh-web-launcher.old.exe

# 运行日志（自动生成）
dsh-web.log

# 本机个人配置（含本机绝对路径，勿提交；仓库提供 config.example.json 模板）
config.json
